- [x] 查找一下claude code的output-format是怎么设置的
  - stream-json 是实时流式输出（token 级），需要配合 --include-partial-messages
  - 每行都是有效 JSON (NDJSON)，适合行流式处理
  - 已更新 PLAN.md 的 Stream JSON 解析部分

- [x] plan模式我们已经讨论过了，但是在PLAN.md中还是不够详细
  - Plan 模式需要使用交互模式（不能用 -p），保持 SSH 连接活跃
  - 通过 stdin 发送用户输入，通过 stdout 接收 stream-json
  - 检测 AskUserQuestion 工具调用来识别需要用户回答的问题
  - 已更新 PLAN.md 的 Plan 模式处理部分
- [x] 既然返回的是消息流，那么问题是，我们怎么解析消息流能得知任务的完成情况，尤其是是否有需要开启定时功能
  - 任务完成：监听 Claude Code 进程退出码（0=成功，非0=失败）
  - 定时功能：在 prompt 中要求 Claude 使用 [WAITING]...[/WAITING] 标记格式
  - 后端解析这个标记，提取等待原因、时间、检查命令
  - 定时任务服务每分钟检查 waiting 任务，到时间后续接
  - 已更新 PLAN.md 的"任务完成与等待检测"部分
- [x] 所有的ui不要用emoji当图标，到网上找一下简单的logo
  - 使用 Lucide Icons (lucide-react)
  - 已更新 PLAN.md 的技术栈部分
